name: V24 Legal Validator Runtime Gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V24 payload staging; push trigger temporarily disabled."
